# Requirements for DEADLOCK-LANDING-PAGE
# Pinned to reasonably-stable versions known to work together in common envs.
# Adjust versions for your environment (CPU vs GPU ONNX runtimes, newer langchain/openai, etc.).

fastapi
uvicorn[standard]
onnxruntime
numpy>=1.26.0
pydantic

# LLM / OpenAI bindings
openai
langchain
# Some code imports `langchain_openai`; if you rely on that package, install it too.
langchain-openai

# Optional helpers
python-dotenv==1.0.0